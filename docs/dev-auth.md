# Dev Auth Flow

Use local Supabase with `supabase/config.toml`.

Dev-safe mode:

- Email signup enabled.
- Email confirmation disabled.
- New users can sign up and sign in immediately.
- No confirmation email required.
- Frontend blocks duplicate auth submits per click and within cooldown window.
